import logging
from openai import AsyncOpenAI
from config_data.config import ConfigEnv, load_config

config: ConfigEnv = load_config()
logger = logging.getLogger(__name__)

client = AsyncOpenAI(api_key=config.openai.api_key)

SYSTEM_PROMPT = """Ты — помощник по созданию объявлений о продаже автомобилей для Telegram-канала в Грузии.

Твоя задача: на основе присланного текста составить объявление СТРОГО по следующему шаблону:

[Марка] [Модель] [Комплектация]
Year: [год]
Mileage: [пробег] km
VIN: [вин-номер]
Engine: [объем] [тип двигателя]
Drive: [привод]
City: [город]

[Описание состояния автомобиля - 2-3 предложения]

💵 [цена]$
📞 [телефон] @[username] (https://t.me/[username])

Правила:
1. Используй ТОЛЬКО информацию из присланного текста
2. Если какой-то информации нет - пропусти эту строку полностью
3. Тип двигателя: turbo, diesel, hybrid, electric - пиши на английском
4. Привод (Drive): FWD, RWD, AWD, 4WD - если указан
5. Описание должно быть кратким и на русском языке
6. Отвечай ТОЛЬКО готовым объявлением, без лишних комментариев
7. НЕ добавляй эмодзи кроме 💵 и 📞

Пример готового объявления:
Audi Q7 Premium Plus 45+
Year: 2022
Mileage: 49000 km
VIN: WA1LJBF79ND017836
Engine: 2.0 turbo
Drive: AWD Quattro
City: Tbilisi

Машина не с аукционов, куплена в салоне США.
Состояние идеальное, работает как часы.
Все оригинальное и заводское.

💵 54000$
📞 +995593470549 @Andreks (https://t.me/Andreks)
"""


async def generate_post_text(user_text: str, correction: str = None) -> str:
    """
    Генерирует текст объявления с помощью GPT-4o-mini
    
    Args:
        user_text: Исходный текст от пользователя
        correction: Дополнительные указания для корректировки
        
    Returns:
        Сгенерированный текст объявления
    """
    try:
        messages = [
            {"role": "system", "content": SYSTEM_PROMPT}
        ]
        
        if correction:
            messages.append({
                "role": "user", 
                "content": f"Исходный текст объявления:\n{user_text}\n\nДополнительные указания для корректировки:\n{correction}"
            })
        else:
            messages.append({
                "role": "user",
                "content": user_text
            })
        
        response = await client.chat.completions.create(
            model="gpt-4o-mini",
            messages=messages,
            temperature=0.7,
            max_tokens=1000
        )
        
        return response.choices[0].message.content
        
    except Exception as e:
        logger.error(f"Ошибка при генерации текста через OpenAI: {e}")
        raise e


